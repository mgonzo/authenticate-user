var test = require('tape');

test('generate token test', function (t) {
    t.plan(1);
    t.equal(false, true);
});

test('validate token test', function (t) {
    // validate correct format
    // validate not expired
    // invalidate expired
    t.plan(1);
    t.equal(false, true);
});
